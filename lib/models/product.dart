class Product {
  final String prodName;
  final String prodImage;
  final double rating;
  final double price;
  final String vendor;
  final bool wishList;

  Product(this.prodName, this.prodImage, this.rating, this.price, this.vendor, this.wishList);
}